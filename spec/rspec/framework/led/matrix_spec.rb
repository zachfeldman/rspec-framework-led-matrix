# frozen_string_literal: true

RSpec.describe Rspec::Framework::Led::Matrix do
  it "has a version number" do
    expect(Rspec::Framework::Led::Matrix::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
